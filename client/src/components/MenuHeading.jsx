import React from "react";

const MenuHeading = ({ children }) => (
  <div className="p-4 font-semibold text-sm uppercase text-neutral-900/60 dark:text-white/60">
    {children}
  </div>
);

export default MenuHeading;
